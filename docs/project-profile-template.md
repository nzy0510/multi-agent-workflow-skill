# Project Profile Template

Copy this file into a repository-specific location and fill it in before using the generic workflow.

## Project Identity

```text
Project name:
Default response language:
Primary target branch:
Repository root:
Remote policy:
```

## Startup Checks

```text
Required first command:
Additional status commands:
Local environment notes:
Known workspace hazards:
```

## Project Areas

Define the repository-specific areas and owners. Avoid assigning the same writable file to multiple roles.

| Area | Typical paths | Default owner | Validation |
| --- | --- | --- | --- |
| `<area-name>` | `<paths>` | `<role>` | `<commands>` |

## Protected Paths and Data

```text
Never commit:
Requires user approval before edit:
Requires specialist review:
Requires release approval:
```

## Validation Commands

```text
Small documentation change:
Core unit checks:
Interface or contract checks:
Integrated checks:
Security or secret scan:
Build or packaging checks:
```

## Role Selection

```text
Default L0 handling:
Default L1 handling:
Default L2 handling:
Default L3 handling:
High-risk additions:
```

## Review Policy

```text
Combined review covers:
Testing review triggers:
Security review triggers:
Maintainability review triggers:
Performance review triggers:
Final review triggers:
```

## Release and Cleanup Policy

```text
Commit message convention:
Publish approval required:
Cleanup approval required:
Local branch cleanup rules:
Workspace cleanup rules:
Release notes location:
```

